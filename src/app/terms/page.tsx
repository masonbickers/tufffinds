import TermsPage from "../(v1)/version-1/terms/page";
import { createVersionOneMetadata } from "../lib/siteMetadata";

export const metadata = createVersionOneMetadata(
  "/terms",
  "Terms of Use — Tufffinds",
);

export default TermsPage;
